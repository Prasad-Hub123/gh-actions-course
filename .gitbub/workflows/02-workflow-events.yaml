name: 02-workflow-events  
on:   
  push:
  pull_request:
  schedule:
    - cron: '*/5 * * * *'  # Runs at midnight every Sunday
    
   
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show Trigger Event
        run: echo "I've been triggered by a(name): ${{ github.event_name }} event."
 